<!doctype html>
<html>
    <head>
        <title>{{ page_title }}</title>
        <meta charset="utf-8">
        {{ content_for_header }}
        <link rel="stylesheet" href="{{  'theme.css' | asset_url }}" type="text/css" media="all">
    </head>
    
    <body>
        {{ content_for_layout }}
    </body>
</html>